steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args: 
      - gcloud
      - functions
      - deploy
      - annotateAPC
      - --runtime=python37
      - --entry-point=main
      - --region=us-central1
      - --source=Serverless/searchAPC
      - --trigger-topic=bigquery-events
      - --update-env-vars=STORAGE_BUCKET=gene222-serverless-demo
